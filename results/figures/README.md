# Curated result figures

The PNG files in this directory are derived from `results/metrics/summary.csv`, which records rounded values from completed repeated experiments. Run `python scripts/generate_result_figures.py` from the repository root to regenerate them.

Smoke tests do not rerun the source-data experiments. They use a small deterministic synthetic matrix to check numerical invariants only.
